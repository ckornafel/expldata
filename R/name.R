# name function(mat){
#   r.name<- rownames(mat)
#   c.name <- colnames(mat)
#   ifelse(is.null(r.name), print("The object does not contain any row names"),
#          print(paste("There are ", length(r.name), "rows with names: ", r.names))
#   ifelse(is.null(c.name), c.name <- "The object does not contain any column names",
#          print(paste(There are))
#
#
# }

