# EDA function(x, expRows = NULL, expCols = NULL, sampSize = 5){
#
#    ########### Error Checks ####################################
#
#   ## Check for supported input types and values
#   if (!(is.data.frame(x) || is.matrix(x) || is.double(x))) {
#     stop("object must be of type: dataframe, matrix")
#   }
#   if(!is.integer(n)){
#     stop("n must be of type: integer")
#   }
#   if(!is.logical(verbose)){
#     stop("verbose must be of type: logical")
#   }
#
#   ########### Function #######################################
#
#   # Local Variables
#
#   #Row Information
#   rowInfo <- RowValid(x, expRows, verbose = FALSE)
#
#   #Column Information
#   colInfo <- ColValid(x, expCols, verbose = FALSE)
#
#   #Dataframe Sample
#   samp <- RowSample(x, sampSize)

    ########### Screen Print ####################################

#



# }
